#include<stdio.h>
#include<stdlib.h>
int a(int n){
	int c;
	c=n%2;
	if(n>=2)
	{
		n=n/2;
		a(n);
	}
	putchar(c==0?'0':'1');
	return 0;
}
int main(){
	int n;
	scanf("%d",&n);
		a(n);
	
	system("pause");
	return 0;
}
